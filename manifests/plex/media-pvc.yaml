apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: smb-media-pvc
  namespace: plex
  labels:
    app.kubernetes.io/managed-by: ngarvey-homelab
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: ""
  volumeName: smb-media-pv
  resources:
    requests:
      storage: 1000Gi
