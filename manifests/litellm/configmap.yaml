apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-config
  namespace: litellm
  labels:
    app.kubernetes.io/managed-by: ngarvey-homelab
data:
  config.yaml: |
    model_list:
      - model_name: local-gpu
        litellm_params:
          model: openai/vllm
          api_base: http://llm.home.arpa:8000/v1
          api_key: sk-unused
    litellm_settings:
      drop_params: true
