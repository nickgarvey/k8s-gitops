---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: acmednscerts.homelab.garvey.sh
spec:
  group: homelab.garvey.sh
  names:
    kind: AcmeDnsCert
    listKind: AcmeDnsCertList
    plural: acmednscerts
    shortNames:
    - adc
    singular: acmednscert
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.dnsName
      name: DNSName
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.fullDomain
      name: FullDomain
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AcmeDnsCert is a request for a per-service Let's Encrypt certificate, issued
          via an acme-dns delegation that the controller provisions automatically.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AcmeDnsCertSpec describes a per-service certificate request fulfilled via an
              acme-dns delegation and cert-manager.
            properties:
              dnsName:
                description: DNSName is the public hostname to obtain a certificate
                  for, e.g. oci.garvey.sh.
                minLength: 1
                type: string
              issuerRef:
                description: IssuerRef references the cert-manager (Cluster)Issuer
                  used to issue the cert.
                properties:
                  group:
                    description: Group of the resource being referred to.
                    type: string
                  kind:
                    description: Kind of the resource being referred to.
                    type: string
                  name:
                    description: Name of the resource being referred to.
                    type: string
                required:
                - name
                type: object
              secretName:
                description: |-
                  SecretName is the name of the Secret (in this namespace) the issued
                  certificate and key are written to by cert-manager.
                minLength: 1
                type: string
            required:
            - dnsName
            - issuerRef
            - secretName
            type: object
          status:
            description: AcmeDnsCertStatus is the observed state of an AcmeDnsCert.
            properties:
              conditions:
                description: 'Conditions: AccountRegistered, CnameReady, Ready.'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              fullDomain:
                description: |-
                  FullDomain is the acme-dns subdomain (<random>.acme.garvey.sh) this host's
                  _acme-challenge CNAME delegates to. Surfaced for visibility/debugging.
                type: string
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last reconciled.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
